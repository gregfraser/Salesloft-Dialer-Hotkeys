# Core service
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
PyYAML>=6.0
psutil>=5.9

# Transcription. faster-whisper uses CTranslate2 and does NOT need torch.
faster-whisper>=1.0

# Voice activity detection. silero-vad's wrapper takes torch tensors even in
# its ONNX mode, so torch is required -- install the CPU-only wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# scripts/install.ps1 does this for you. The default PyPI wheel pulls ~2.5GB
# of CUDA libraries that this service will never use.
silero-vad>=5.1
torch>=2.0

# Test-only and tooling
pytest>=8.0
httpx>=0.27
websockets>=12.0   # scripts/replay_wav.py
